---
interface Props{
    class?: string;
}
---

<div class={
    `${Astro.props.class} rounded-full bg-[#27242b] flex justify-center items-center hover:scale-125 ease-in-out duration-300 `
}>
    <slot/>
</div>